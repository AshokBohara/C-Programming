#include<stdio.h>
int main()
{
	float a,b,c
	printf("enter_three_side_of_triangle")
	scanf("%f%f%f",&a,&b,&c);
	if((a+b>c)&&(b+c>a)&&(c+a>b))
	{
    if(a==b&b==c)
    {
    printf("the triangle is equilateral");
    {
    else
    if (a==b||b==c||a==c)
    {
    printf("the triangle is isoceles");
    {
   	else
   	{
   	printf("the triangle is scelence");
   	{
   	else	
	   }
   	printf("the triangle is invalid");
   	return(0);
   }
	}		
	}
	}
}
