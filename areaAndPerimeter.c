#include<stdio.h>
int main()
{
    float leangth,breadth,area,perimeter;
    pfintf(enter leangth and breadth);
    scanf(%f%f,and leangth and breadth );
    area=length*breadth;
    perimeter=2*(ledangth=breadth);
    printf(area of rectangle=%f;area);
    pruntf(perimeter of rectangle=%fperimeter);
    return(0)
}
